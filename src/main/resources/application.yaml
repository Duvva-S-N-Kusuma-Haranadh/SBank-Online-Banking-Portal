spring:
  application:
    name:
       SBank

  datasource:
    url: jdbc:mysql://localhost:3306/sbank
    username: root
    password: S+p@100%
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
    properties:
      hibernate:
        query:
          startup_check: false

server:
  port: 8080

jwt:
  secret: mysecretkey123456789012345678901234567890
